name: Release-plz

# Automated releases driven by conventional commits.
#
#   - release-plz-pr:      opens/updates a "release PR" that bumps the workspace
#                          version and regenerates the changelogs.
#   - release-plz-release: on push to main (i.e. when that PR is merged), tags
#                          the release, creates the GitHub Release(s), and
#                          publishes the changed crates to crates.io.
#
# Auth:
#   - GitHub:   the default GITHUB_TOKEN (no PAT/App, so no org-admin setup).
#               Tradeoff: PRs opened by GITHUB_TOKEN do not trigger CI, so the
#               release PR won't run the CI workflow. That's acceptable here --
#               it only bumps versions/changelogs, and the feature PRs feeding
#               the release already passed CI. To get CI on the release PR,
#               swap in a PAT/App token later.
#   - crates.io: OIDC trusted publishing (no stored token) -- see the
#               `id-token: write` permission on the release job. Configure each
#               crate's trusted publisher at crates.io/crates/<name>/settings.
#
# NOTE: release-plz/action is pinned by commit SHA (the `# v0.5.130` comment
# tracks the human-readable tag). Do NOT replace the SHA with the tag form:
# that token looks like an email address and gets mangled by an obfuscation
# filter into an invalid `uses:` ref, which makes GitHub reject the workflow.

on:
  push:
    branches:
      - main

# No ambient permissions; each job opts into exactly what it needs.
permissions: {}

jobs:
  # Publish any unreleased versions to crates.io and create GitHub Releases.
  release-plz-release:
    name: Release-plz release
    runs-on: ubuntu-latest
    permissions:
      contents: write   # push tags + create GitHub Releases
      id-token: write   # OIDC: mint a short-lived crates.io token (trusted publishing)
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history is needed to release from the merge commit
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run release-plz
        uses: release-plz/action@e8792575c7f2366cf6ff3ccc33ead9ace5b691c7 # v0.5.130
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # No CARGO_REGISTRY_TOKEN: release-plz exchanges the OIDC token from
          # `id-token: write` above for a short-lived crates.io token itself.

  # Open/update the release PR (version bump + changelog) for the next release.
  release-plz-pr:
    name: Release-plz PR
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    # Don't cancel an in-flight PR update; let it finish to avoid a half-written PR.
    concurrency:
      group: release-plz-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run release-plz
        uses: release-plz/action@e8792575c7f2366cf6ff3ccc33ead9ace5b691c7 # v0.5.130
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
